{
  "name": "rws-app-proposal-monitor",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "serve": "browser-sync start --config bs-config.js",
    "build": "bash -c '[[ $BUILD_SERVER == \"true\" ]] && node ./src/build.js || nodemon'"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "nodemon": "^3.1.0"
  },
  "dependencies": {
    "browser-sync-webpack-plugin": "^2.3.0",
    "lodash": "^4.17.21"
  }
}
